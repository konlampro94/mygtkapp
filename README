To build and install this program run:


username=`whoami` | exit 1;

./autogen.sh --prefix=/home/$username/.local

make install

-----------
Running the first line above creates the following files:



To uninstall

-----------
make uninstall